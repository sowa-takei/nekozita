<h4>商品新規登録</h4>
<%= form_with model: @item,url:admin_items_path(@item),local:true do |f| %>
<table>
  <tr>
    <th><label>商品画像</label></th>
    <td><%= f.attachment_field :image %></td>
  </tr>
  <tr>
    <th><label>商品名</label></th>
    <td><%= f.text_field :name %></td>
  </tr>
  <tr>
    <th>商品説明</th>
    <td><%= f.text_area :introductiion %></td>
  </tr>
  <tr>
    <th>ジャンル</th>
    <td><%= f.</td>
  </tr>


</table>
<% end %>