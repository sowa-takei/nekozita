<div class="container">
  <div class="row">
    <div class="col-2">
      <table>
        <tr>
         <th>BRAND</th>
        </tr>
        <tr>
          <td>nekozita</br>
              Off-White</br>
              HERON PRESTON</br>
              WE11DONE</br>
              BALENCIAGA</br>
              DITA
           </td>
        </tr>
      </table>
    </div>
    <div class="col-10">

    　<h4>新着商品</h4>
    　<div class="row mx-auto mt-3">
    　<% if @search_items %>
        <%# render partial: 'items/item', collection: @search_items %>
        <% @search_items.last(8).each do |item| %>
          <div class="mx-auto">
           <%= link_to item_path(item) do %>
             <%= attachment_image_tag item, :image, :fill,200,200 %></br>
             <div class="row mt-3">
               <%= item.name %></br>
             </div>
             <div class="row mt-1">
              ￥<%= item.price %>
             </div>
           <% end %>
          </div>
        <% end %>
      <% else %>
      <% @items.last(8).each do |item| %>
       <div class="mx-auto">
       <%= link_to item_path(item) do %>
      <%= attachment_image_tag item, :image, :fill,100,100 %></br>
       <div class="row mt-3">
        <%= item.name %></br>
       </div>
       <div class="row mt-1">
        ￥<%= item.price %>
       </div>
       <% end %>
     </div>
    <% end %>
   <% end %>
   </div>
  </div>
 </div>
</div>
